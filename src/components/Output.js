import React from 'react'

const Output = (props) => {
  return (
    <div>
      console.log({props.info})
    </div>
  )
}

export default Output
  // {props.result.map((element, index) => {
  //   <div>{element}</div>
  // })}
